---

- name: Reboot host
  shell: shutdown -r now
  async: 1
  poll: 0
  sudo: true
  ignore_errors: true

- name: Waiting for server to come back
  local_action: wait_for host={{ inventory_hostname }} state=started delay=60 timeout=600
  sudo: false