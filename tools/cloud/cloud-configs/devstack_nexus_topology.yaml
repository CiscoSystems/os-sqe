servers:
  devstack-server:
    params:
      hostname:
      count: 2
      storage: 60
      add_disks:
      ram: 8
      cpu: 1
      networks:
        - admin
        - bridge
networks:
  - admin:
      dhcp: true
      external: false
      nat: true
      dns:
        devstack: 2
  - bridge:
      - eth1
      - eth0